{% set name = "Quart-CORS" %}
{% set version = "0.1.1" %}
{% set sha256 = "605d4239ab89c216c87165653cbd5405f508e68674ef133a40176ce464ef52c7" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  fn: {{ name }}-{{ version }}.tar.gz
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  noarch: python
  number: 0
  script: python -m pip install --no-deps --ignore-installed .

requirements:
  host:
    - pip
    - python >=3.6.1
    - setuptools >=40.6.2

  run:
    - python >=3.6.1
    - Quart >=0.6.0

test:
  imports:
    - quart_cors

about:
  home: https://github.com/theelous3/asks
  license: MIT
  license_family: MIT
  summary: 'A Quart extension to provide Cross Origin Resource Sharing, access control, support.'
  description: |
    Quart-CORS is an extension for Quart to handle Cross Origin Resource
    Sharing, CORS (also known as access control).
  doc_url: https://gitlab.com/pgjones/quart-cors
  dev_url: https://gitlab.com/pgjones/quart-cors

extra:
  recipe-maintainers:
    - maxyme
